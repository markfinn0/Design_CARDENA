name: Copy All Files

on:
  push:
    branches:
      - main  # Ajuste para a branch principal do repositório Design_CARDENA
    paths:
      - '**/*'

jobs:
  copy_all_files:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Design_CARDENA Repository
      uses: actions/checkout@v2
      with:
        repository: markfinn0/Design_CARDENA  # Ajuste para o seu repositório Design_CARDENA

    - name: Checkout teste_mobile Repository
      uses: actions/checkout@v2
      with:
        repository: markfinn0/teste_mobile  # Ajuste para o seu repositório teste_mobile

    - name: Copy All Files
      run: |
        cp -r Design_CARDENA/* teste_mobile/

    - name: Configure Git for teste_mobile Repository
      run: |
        cd teste_mobile
        git config user.name "GitHub Actions"
        git config user.email "actions@github.com"

    - name: Commit and Push to teste_mobile
      run: |
        git add .
        git commit -m "Copy all files from Design_CARDENA"
        git push origin HEAD:refs/heads/main  # Ajuste para a branch principal do repositório teste_mobile
